apiVersion: v1
kind: Service
metadata:
  name: topic
  labels:
    app: topic
    service: topic
spec:
  ports:
  - port: 8080
    targetPort: 8000
    name: http
  selector:
    app: topic
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: circuit-breaker-for-topic-service
spec:
  host: "topic.default.svc.cluster.local"
  trafficPolicy:
    #2. Circuit Breaker가 작동하는 기준을 설정
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 20s
      baseEjectionTime: 30s
      maxEjectionPercent: 100
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: topic
spec:
  hosts:
  - "topic.default.svc.cluster.local"
  http:
  - fault:
      abort:
        httpStatus: 500
        percentage:
          value: 100
    match:
    - headers:
        end-user:
          exact: test
    route:
    - destination:
        host: "topic.default.svc.cluster.local"
        subset: rtopic
  - route:
    - destination:
        host: "topic.default.svc.cluster.local"
